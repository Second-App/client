export const SET_T_TYPES = (payload) => {
  return {
    type: 'transactions/types',
    payload
  }
}

export const SET_ONE_T_TYPE = (payload) => {
  return {
    type: 'transactions/oneTypes',
    payload
  }
}