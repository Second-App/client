export const SET_CATEGORIES = (payload) => {
  return {
    type: 'categories/setCategories',
    payload
  }
}

export const SET_ONE_CATEGORY = (payload) => {
  return {
    type: 'category/category',
    payload
  }
}